const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-camera-preview-state'));return boards.map(b=>{const label=penpotUtils.findShape(s=>s.name==='Stop Video label',b);if(!label.textBounds?.width)throw Error('Unrendered Stop Video label '+b.name);label.x=label.parent.x+(label.parent.width-label.textBounds.width)/2;return {id:b.id,labelBounds:label.textBounds};});
