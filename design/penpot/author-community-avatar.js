if(penpot.currentPage.name!=='Desktop — Main')throw Error('Wrong page');const keys=['image','fallback','uploaded','removed'],key=keys.find(k=>!penpot.currentPage.root.children.some(b=>b.name==='Desktop / Settings / community-avatar-'+k));if(!key)return {remaining:0};const base=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / community-general-default'),b=base.clone(),font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');b.name='Desktop / Settings / community-avatar-'+key;b.x=keys.indexOf(key)*1400;b.y=0;b.setPluginData('static-community-general-loaded','');const c=penpotUtils.findShape(s=>s.name==='General scroll content',b);
function box(name,x,y,w,h,fill,radius=0){const s=penpot.createBoard();s.name=name;s.resize(w,h);s.fills=[{fillColor:fill,fillOpacity:1}];s.borderRadius=radius;c.appendChild(s);s.x=c.x+x;s.y=c.y+y;return s;}
function text(name,copy,x,y,w,size,weight,fill){const t=penpot.createText(copy);t.name=name;font.applyToText(t,font.variants.find(v=>v.fontWeight===String(weight)&&v.fontStyle==='normal'));t.fontSize=String(size);t.resize(w+24,size*1.5);t.growType='auto-width';t.fills=[{fillColor:fill,fillOpacity:1}];c.appendChild(t);t.x=c.x+x;t.y=c.y+y;t.setPluginData('general-loaded-target',JSON.stringify({x,y}));return t;}
if(['image','uploaded'].includes(key)){c.children.find(s=>s.name==='span text 1').remove();const avatar=c.children.find(s=>s.name==='span community-settings-avatar');avatar.name='Synthetic Community avatar';avatar.fills=[{fillColor:'#2563EB',fillOpacity:1}];box('Avatar white circle',209,12,30,30,'#FFFFFF',15);box('Avatar orange stripe',209,47,30,5,'#F5A623',2.5);}
if(key!=='removed'){const r=box('button Remove avatar',582,17.796875,129,28.390625,'#6B6B6B');r.strokes=[{strokeColor:'#FFFFFF',strokeOpacity:1,strokeWidth:2,strokeStyle:'solid',strokeAlignment:'inner'}];text('Remove avatar label','Remove avatar',590,21.796875,113,16,400,'#FFFFFF');}
if(['uploaded','removed'].includes(key)){c.children.find(s=>s.name==='file content avatar No file chosen').remove();text('Avatar selected filename','avatar.png',369,34.953125,110,12.8,700,'#A4A8B3');}
b.setPluginData('static-community-avatar-state',key);b.showInViewMode=false;for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);penpot.viewport.zoomIntoView([b]);return {key,id:b.id,remaining:keys.filter(k=>!penpot.currentPage.root.children.some(b=>b.name==='Desktop / Settings / community-avatar-'+k)).length};
