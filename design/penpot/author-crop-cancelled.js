if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const results=[];for(const [i,kind] of ['avatar','banner'].entries()){const key=kind+'-crop-cancelled',name='Desktop / Settings / '+key;let b=penpot.currentPage.root.children.find(s=>s.name===name);if(!b){const source=penpot.currentPage.root.children.find(s=>s.name==='Desktop / Settings / '+kind+'-crop');if(!source)throw Error('Missing crop base');b=source.clone();b.name=name;b.x=25400+i*1400;b.y=0;penpotUtils.findShape(s=>s.name==='Image crop dialog',b).remove();b.setPluginData('static-profile-crop-state','');b.setPluginData('static-crop-cancelled-state',key);b.showInViewMode=false;for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}results.push({key,id:b.id});}return results;
