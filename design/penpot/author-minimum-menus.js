if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const specs=[['microphone','voice-minimum','Microphone device'],['speaker','voice-minimum','Speaker device'],['suppression','voice-minimum-processing','Noise suppression'],['camera','voice-minimum-camera','Camera device'],['quality','voice-minimum-bottom','Screen share quality']],results=[];let created=0;
for(const [i,[key,baseKey,controlName]] of specs.entries()){let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-small-menu-'+key);if(!b&&created<3){const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+baseKey),reference=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-menu-'+key);if(!source||!reference)throw Error('Missing prerequisite');b=source.clone();b.name='Desktop / Settings / voice-small-menu-'+key;b.x=120000+i*1100;b.y=0;b.setPluginData('static-voice-minimum-state','');b.showInViewMode=false;const control=penpotUtils.findShape(s=>s.name===controlName,b);control.strokes=[{strokeColor:'#6D75E8',strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];control.shadows=[{style:'drop-shadow',offsetX:0,offsetY:0,blur:0,spread:2,color:{color:'#6D75E8',opacity:.24}}];const focus=penpotUtils.findShape(s=>s.name==='Native select focus',reference).clone();b.appendChild(focus);focus.resize(control.width+10,48);focus.x=control.x-5;focus.y=control.y-5;
const menu=penpotUtils.findShape(s=>s.name==='Native voice select menu',reference).clone();b.appendChild(menu);menu.resize(control.width,menu.height);menu.x=control.x;menu.y=control.y+control.height;for(const t of menu.children){if(t.name==='Selected native option')t.resize(control.width-2,24);else if(t.type==='text')t.resize(control.width-20,18);}b.setPluginData('static-minimum-menu-state',key);b.setPluginData('menu-control',controlName);b.setPluginData('menu-options',reference.getPluginData('menu-options'));for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);created++;}if(b)results.push({key:'voice-small-menu-'+key,id:b.id});}return {created,remaining:specs.length-results.length,boards:results};
