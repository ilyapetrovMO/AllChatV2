if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / notifications-default');if(!source)throw Error('Missing notification base');const keys=['notifications-minimum','notifications-minimum-scrolled','notifications-hover-community','notifications-hover-channel'],results=[];for(const [i,key] of keys.entries()){const name='Desktop / Settings / '+key;let b=penpot.currentPage.root.children.find(b=>b.name===name);if(!b){b=source.clone();b.name=name;b.x=52400+(i%2)*1400;b.y=Math.floor(i/2)*960;b.setPluginData('static-notification-settings-state','');b.showInViewMode=false;const find=n=>penpotUtils.findShape(s=>s.name===n,b);if(key.includes('minimum')){b.resize(960,640);find('Desktop title bar').resize(960,28);for(const n of ['Window minimize','Window maximize','Window close'])find(n).x-=320;find('Community rail').resize(72,612);find('Conversation sidebar').resize(240,612);for(const n of ['Current member panel','Add community','Add icon'])find(n).y-=160;find('Channel header').resize(648,48);find('Settings title').resize(616,22.4);find('Settings viewport').resize(648,564);const c=find('Settings scroll content');c.resize(584,1050);for(const s of penpotUtils.findShapes(()=>true,c))if(s.width>312)s.resize(s.width-312,s.height);const old=find('Notification description'),t=penpot.createText('Choose when this Community may notify you. Electron delivers native\noperating-system notifications.'),font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');t.name=old.name;font.applyToText(t,font.variants.find(v=>v.fontWeight==='400'&&v.fontStyle==='normal'));t.fontSize='16';t.resize(584,44.8);t.fills=[{fillColor:'#F0F1F5',fillOpacity:1}];c.appendChild(t);t.x=old.x;t.y=old.y;old.remove();for(const n of ['Desktop notifications card','Community defaults card','Channel overrides card'])find(n).y+=22.4;const overrides=find('Channel overrides card');find('Channel level general').x=overrides.x+269;find('Channel mute general').x=overrides.x+501;find('Channel mute general label').x=overrides.x+528;find('Channel mute general label').resize(50,20.16);const arrow=find('Community notification level arrow');arrow.x=arrow.parent.x+arrow.parent.width-20;if(key.endsWith('scrolled'))c.y-=194;}else{const target=find(key.endsWith('community')?'Community notification level':'Channel level general');target.strokes=[{strokeColor:'#FFFFFF',strokeOpacity:.18,strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];}for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);b.setPluginData('static-notification-layout-state',key);}results.push({key,id:b.id});}return results;
