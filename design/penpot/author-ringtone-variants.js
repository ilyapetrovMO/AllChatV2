const font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');function replace(b,name,copy){const old=penpotUtils.findShape(s=>s.name===name,b);const t=penpot.createText(copy);t.name=name;font.applyToText(t,font.variants.find(v=>v.fontWeight===old.fontWeight&&v.fontStyle==='normal'));t.fontSize=old.fontSize;t.resize(old.width,old.height);t.fills=old.fills;old.parent.appendChild(t);t.x=old.x;t.y=old.y;old.remove();}
const results=[];for(const [i,[key,source]] of [['ringtone-error-custom','ringtone-saved'],['ringtone-reset-generated','ringtone-reset'],['ringtone-volume-half','ringtone-generated']].entries()){const name='Desktop / Settings / '+key;let b=penpot.currentPage.root.children.find(b=>b.name===name);if(!b){const base=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+source);if(!base)throw Error('Missing base');b=base.clone();b.name=name;b.x=37400+(i%2)*1400;b.y=Math.floor(i/2)*960;b.setPluginData('static-ringtone-state','');b.setPluginData('static-ringtone-variant',key);b.showInViewMode=false;if(key==='ringtone-error-custom')replace(b,'Ringtone notice','Could not save ringtone.');if(key==='ringtone-reset-generated')replace(b,'Ringtone source','Using the generated tone.');if(key==='ringtone-volume-half'){replace(b,'Volume output','50%');const track=penpotUtils.findShape(s=>s.name==='Ringtone volume track',b);track.fills=[{fillColor:'#555968',fillOpacity:1}];const fill=track.clone();fill.name='Ringtone volume filled range';fill.resize(421,6);track.parent.appendChild(fill);fill.x=track.x;fill.y=track.y;fill.fills=[{fillColor:'#6D75E8',fillOpacity:1}];const thumb=penpotUtils.findShape(s=>s.name==='Ringtone volume thumb',b);thumb.x=track.x+413;thumb.parent.appendChild(thumb);}for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}results.push({key,id:b.id});}return results;
