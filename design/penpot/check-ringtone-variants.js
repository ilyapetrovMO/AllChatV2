const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-ringtone-variant')),problems=[];for(const b of boards){const key=b.getPluginData('static-ringtone-variant'),s=penpotUtils.findShapes(()=>true,b),labels=s.filter(s=>s.type==='text').map(s=>s.characters);const expected=key==='ringtone-error-custom'?['Custom audio is active.','Could not save ringtone.','Use Community default']:key==='ringtone-reset-generated'?['Using the generated tone.','Using the Community ringtone.']:['50%'];for(const label of expected)if(!labels.includes(label))problems.push(key+': missing '+label);if(key==='ringtone-reset-generated'&&labels.includes('Use Community default'))problems.push('Unexpected reset button');if(s.some(s=>s.interactions.length))problems.push('Interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===3&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
