const b=penpot.currentPage.root.children.find(b=>b.getPluginData('static-safety-dialog-state')==='safety-delete-confirm');if(!b)return {pass:false,problems:['Missing board']};const shapes=penpotUtils.findShapes(()=>true,b),d=shapes.find(s=>s.name==='Native Linux confirmation reference'),t=shapes.find(s=>s.name==='Confirmation question'),problems=[];if(d.width!==320||d.height!==81)problems.push('Dialog geometry');if(t?.characters!=='Permanently anonymize this Account?'||!t?.textBounds?.width)problems.push('Question rendering');if(t.textBounds.x+t.textBounds.width>d.x+d.width)problems.push('Question overflow');for(const label of ['Ok','Cancel']){const btn=shapes.find(s=>s.name===label+' native button'),s=shapes.find(s=>s.name===label+' native label');if(!s?.textBounds?.width||s.characters!==label)problems.push(label+' missing');if(Math.abs(s.x+s.textBounds.width/2-btn.x-btn.width/2)>1)problems.push(label+' centering');}if(shapes.some(s=>s.interactions.length)||b.interactions.length)problems.push('Interactions');const validation=penpot.currentFile.validate();return {pass:!problems.length&&!validation.length,problems,validation,questionBounds:t.textBounds};
