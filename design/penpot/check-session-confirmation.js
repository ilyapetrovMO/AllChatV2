const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-session-confirmation-state')),problems=[];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),labels=shapes.filter(s=>s.type==='text').map(s=>s.characters);if(b.name.endsWith('sessions-revoked')){if(labels.includes('Revoke')||labels.includes('Laptop Windows'))problems.push('Revoked session remains');if(!labels.includes('Current Session'))problems.push('Current session removed');}else for(const label of ['Revoke the Session on Laptop Windows?','Ok','Cancel'])if(!labels.includes(label))problems.push('Missing '+label);if(shapes.some(s=>s.interactions.length))problems.push('Interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===2&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
