const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-settings-state')),problems=[];const expected=['Voice','Microphone','Speaker','Microphone volume','Speaker volume','Mic Test','Input processing','Noise suppression','Standard','Echo cancellation','Automatic gain control','Noise gate','Input sensitivity','-50 dB','Camera','Camera preview is off','Test Video','Screen sharing','Quality mode','Auto','Advanced','Speaker test','Play sound','Reset Voice & Video settings','Reset'];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),labels=shapes.filter(s=>s.type==='text').map(s=>s.characters);for(const label of expected)if(!labels.includes(label))problems.push(b.name+': missing '+label);for(const [name,on] of [['Echo cancellation',true],['Automatic gain control',false],['Noise gate',true]])if(shapes.some(s=>s.name===name+' tick')!==on)problems.push('Wrong toggle '+name);if(shapes.some(s=>s.type==='text'&&!s.textBounds?.width))problems.push(b.name+': unrendered text');if(shapes.some(s=>s.interactions.length))problems.push('Interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===4&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
