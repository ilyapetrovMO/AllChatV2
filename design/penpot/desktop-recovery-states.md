# Desktop account recovery

Source: `desktop/src/renderer/app.tsx:129` and `:398`. Recovery requires a recovery token and new password; the password is masked, required, minLength=12, and autocomplete=new-password. The submit label is “Replace password”. There is no dedicated pending label or password visibility toggle.

Submission clears the existing alert, calls bridge.recoverInstance, then switches to Sign in with “Password replaced. Sign in with your new password.” Failures display the thrown message or “Could not recover the Account.” Editing alone does not clear the alert.

The existing board 6bdc1c40-ce96-804f-8008-9a647072d59b now has shared blank input/button instances, selected Recovery/idle authentication tabs, selected community icon/marker, uppercase labels, centered heading/address and source-derived radial background/card shadow. `correct-desktop-recovery.js` records construction. Separate style and text passes avoid inherited component placeholders. `desktop-recovery-live.svg` and `desktop-recovery-render.png` contain the locally reviewed default state. Typography still differs from the captured platform rendering; hosted export/persistence and token checks remain open.

Remaining: capture focus/hover, filled/masked inputs, required/minimum-length validation, invalid token, editing after error and successful replacement-to-sign-in. Reconstruct those editable states and map native validation references. Verify responsive behavior and the desktop-wide gates before marking this screen complete.

Recovery capture preparation: `capture-desktop-recovery.mjs` is wired through `captureDesktop(...,{recoveryOnly:true})`; syntax checks pass. It captures default, focus/hover, native required/short-password, masked filled, invalid token and retained-error editing states. It has not run: the local fixture at 127.0.0.1:4187 returned no HTTP response on the latest check. Successful recovery remains separate work requiring a disposable member/token, not the fixture owner.

## Captured interaction and error states

The fixture outage was a sandbox visibility issue. An elevated listener/process check confirmed the existing disposable server on port 4187 (data directory /tmp/allchat-design-aLQ6W0); it was reused. Recovery capture completed in Electron via xvfb-run. Thirteen references now include Add Instance/sign-in prerequisites and eleven recovery states; native validation confirms missing token/password and a five-character password failing minLength=12. Captured error copy is in `populated/desktop-recovery-reference/error-copy.json`.

Seven editable state boards are recorded in `desktop-recovery-state-boards.json`, constructed by `author-desktop-recovery-states.js`. Input styles and text overrides were applied separately. Password values use 28 masking bullets. Error card geometry is y=70.88, height=606.25, with the exact three-line captured alert. Local SVG/PNG renders are in `desktop-recovery-state-renders/`; invalid-token and filled-state renders were visually inspected. Other state renders still need individual comparison. Platform typography, persisted export and token propagation remain open.

Remaining recovery work: create the resulting sign-in confirmation state, connect flow transitions, place native validation references, and finish responsive/fidelity verification. Successful reset capture is present; see the current evidence review below.

Render correction: first inspection exposed stale blank/placeholder component text despite updated API characters. Repeating separate fill and text override calls refreshed layout; all seven SVGs and PNGs were regenerated. The corrected filled render was inspected again. This recurring discrepancy remains relevant to the final export/persistence gate.

## Current evidence review — 2026-09-08

The capture completed further than the earlier notes recorded. `populated/desktop-recovery-reference/manifest.json` includes `valid-token-filled`, `recovery-success-sign-in`, and `recovered-member-home`. `success.json` records successful replacement-password login with a dedicated disposable member. The current capture script asserts the confirmation text and authenticated welcome view before writing that result. The success screenshot was inspected: it returns to blank sign-in inputs and displays the two-line confirmation below the submit button. No recapture was performed during this review.

`author-desktop-recovery-success.js` prepares the editable confirmation state from the existing sign-in baseline, retaining its reusable controls. Captured card bounds are x=436, y=64.08, width=480, height=619.84; the selected Sign in tab starts at y=291.23. The script passed JavaScript parsing, but has **not been executed in Penpot**. It has no verified board ID or rendered output yet.

Five existing local renders were individually compared with their corresponding Electron PNGs: `token-focused`, `password-focused`, `submit-keyboard-focused`, `submit-hovered`, and `recovery-edited-after-error`. Focus rings, selected Recovery tab, hover color and card placement appear aligned. Typography does not pass fidelity: the heading, field labels, tabs, submit label and input contents are visibly narrower in the design; masked password dots occupy less width. The edited-error state retains the correct alert and has three lines, but their widths and baselines also differ. These are visual observations of local files, not a fresh hosted rendering or persistence check.

The local bridge was restarted successfully. Two direct connection checks reported “No Penpot plugin instances are currently connected.” Browser-plugin reconnection is required for applying the prepared state and further hosted verification. No screens were marked complete. The desktop-wide typography, token propagation, native export/reimport and responsive gates remain open.

Follow-up review: the connection still reports no plugin. The remaining `recovery-filled` and `invalid-token-error` local render/reference pairs were inspected, completing this comparison pass across all seven existing recovery state boards. They have the expected values, focus state and alert content, but the same typography mismatch prevents fidelity approval. `desktop-rendered-fonts.json` records actual Electron glyph rendering with DejaVu Sans (regular and bold), despite the CSS stack starting with Inter. The saved Penpot font query in `desktop-font-options.json` found Inter but no DejaVu Sans. Recheck available fonts after reconnecting and supply the captured platform font before correcting text geometry; narrowing the source reference or converting text to outlines would not satisfy the as-built/editable-text requirements. This diagnosis applies to the captured Linux reference, not every OS.
