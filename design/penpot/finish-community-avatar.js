const keys=['image','fallback','uploaded','removed'],out=[];for(const [i,key]of keys.entries()){const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / community-avatar-'+key);if(!b)throw Error('Missing '+key);const c=penpotUtils.findShape(s=>s.name==='General scroll content',b);for(const t of c.children.filter(s=>s.type==='text'&&s.getPluginData('general-loaded-target'))){const m=JSON.parse(t.getPluginData('general-loaded-target'));t.x+=c.x+m.x-t.textBounds.x;t.y+=c.y+m.y-t.textBounds.y;}b.x=243000+i*1400;b.y=0;out.push({key:'community-avatar-'+key,id:b.id});}return out;
