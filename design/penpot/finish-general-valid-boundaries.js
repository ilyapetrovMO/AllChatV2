const out=[];for(const [i,key]of ['minimum','maximum'].entries()){const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / general-valid-'+key);if(!b)throw Error('Missing '+key);for(const t of penpotUtils.findShapes(s=>s.type==='text'&&s.getPluginData('boundary-target'),b)){const m=JSON.parse(t.getPluginData('boundary-target')),p=penpot.currentPage.getShapeById(m.parent);t.x+=p.x+m.x-t.textBounds.x;t.y+=p.y+m.y-t.textBounds.y;}b.x=267000+i*1400;b.y=0;out.push({key:'general-valid-'+key,id:b.id});}return out;
