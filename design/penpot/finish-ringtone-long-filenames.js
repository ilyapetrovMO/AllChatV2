const out=[];for(const [i,key]of ['wide','small'].entries()){const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / ringtone-long-filename-'+key);if(!b)throw Error('Missing '+key);b.x=345000+i*1400;b.y=0;const c=penpotUtils.findShape(s=>s.name==='General scroll content',b);for(const t of c.children.filter(s=>s.type==='text'&&s.getPluginData('general-loaded-target'))){const m=JSON.parse(t.getPluginData('general-loaded-target')),dx=c.x+m.x-t.textBounds.x,dy=c.y+m.y-t.textBounds.y;if(Math.abs(dx)>.1)t.x+=dx;if(Math.abs(dy)>.1)t.y+=dy;}out.push({key:'ringtone-long-filename-'+key,id:b.id});}return out;
