const out=[];for(const [i,key] of ['report','moderation','delete'].entries()){const b=penpot.currentPage.root.children.find(b=>b.getPluginData('static-safety-input-state')===key);if(!b)throw Error('Missing '+key);for(const s of penpotUtils.findShapes(s=>s.getPluginData('safety-entered-field'),b))if(!s.textBounds?.width)throw Error('Unrendered '+s.name);b.x=149000+i*1400;b.y=0;out.push({key:'safety-filled-'+key,id:b.id});}return out;
