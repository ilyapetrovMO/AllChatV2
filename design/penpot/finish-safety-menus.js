const out=[];for(const [i,key] of ['report','action','member'].entries()){const b=penpot.currentPage.root.children.find(b=>b.getPluginData('static-safety-menu-state')===key);if(!b)throw Error('Missing '+key);const menu=penpotUtils.findShape(s=>s.name==='Safety native menu',b),control=penpotUtils.findShape(s=>s.name===b.getPluginData('menu-control'),b);control.strokes=[{strokeColor:'#FFFFFF',strokeOpacity:.14,strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];for(const t of menu.children.filter(s=>s.type==='text'))if(!t.textBounds?.width)throw Error('Blank '+t.name);b.x=144000+i*1400;b.y=0;out.push({key:'safety-menu-'+key,id:b.id});}return out;
