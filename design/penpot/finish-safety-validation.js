const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-safety-validation-state')),keys=['report-member','report-reason','report-short','moderation-reason','duration','password','confirmation','confirmation-pattern'],out=[];let changed=false;for(const key of keys){const b=boards.find(b=>b.getPluginData('static-safety-validation-state')===key);if(!b)throw Error('Missing '+key);if(!b.getPluginData('safety-validation-positioned')&&!changed){const t=penpotUtils.findShape(s=>s.name==='Native validation message',b);if(!t.textBounds?.width)throw Error('Blank '+key);if(key==='report-short')t.resize(t.width,40);b.x=154000+keys.indexOf(key)*1400;b.y=0;b.setPluginData('safety-validation-positioned','yes');changed=true;}out.push({key:'safety-invalid-'+key,id:b.id});}return {changed,remaining:boards.filter(b=>!b.getPluginData('safety-validation-positioned')).length,boards:out};
