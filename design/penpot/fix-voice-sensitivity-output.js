const font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-settings-state'));for(const b of boards){const old=penpotUtils.findShape(s=>s.name==='Input sensitivity output',b);const t=penpot.createText('-50 dB');t.name=old.name;font.applyToText(t,font.variants.find(v=>v.fontWeight==='700'&&v.fontStyle==='normal'));t.fontSize='13';t.resize(60,18.2);t.fills=[{fillColor:'#A4A8B3',fillOpacity:1}];old.parent.appendChild(t);t.x=old.x;t.y=old.y;old.remove();}return {fixed:boards.length};
