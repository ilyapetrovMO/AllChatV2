# Native file-picker capture remains unresolved

The isolated Electron/Linux Xvfb capture showed only the input and no picker. Repeated with a longer paint delay, then direct dialog.showOpenDialog with image filters and an isolated fixture directory. Each process exited normally after its scheduled screenshot, but none produced a visible dialog or a completed picker result. No live capture process remains. This does not establish that the app has no picker or that the user-facing picker is broken; it is a capture limitation needing further investigation.

Do not count no-visible-dialog.png as a picker design. No screenshot was uploaded. Continue other authorized static design work and retain the missing native-picker reference in the coverage ledger. The script is a diagnostic fixture, not a prototype; it uses workspace-specific paths and its output path must be updated before re-running.
