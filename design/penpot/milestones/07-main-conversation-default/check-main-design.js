const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Main / Text channel');if(!b)throw Error('Missing board');const shapes=penpotUtils.findShapes(()=>true,b),text=shapes.filter(s=>s.type==='text');return {fileId:penpot.currentFile.id,pageId:penpot.currentPage.id,revision:penpot.currentFile.revn,boardId:b.id,nativeTextLayers:text.length,unrendered:text.filter(s=>!s.textBounds?.width).map(s=>s.name),regions:shapes.filter(s=>['Community rail','Conversation sidebar','Channel header','Message workspace','Member directory','Message composer'].includes(s.name)).map(s=>({name:s.name,x:s.x-b.x,y:s.y-b.y,width:s.width,height:s.height})),validation:penpot.currentFile.validate()};
