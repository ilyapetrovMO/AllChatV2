const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Main / Popover / community-member');for(const [name,value] of [['Current member name','Sam'],['Current member handle','@sam']])penpotUtils.findShape(s=>s.name===name,b).characters=value;const avatar=penpotUtils.findShape(s=>s.name==='Current member avatar',b);penpotUtils.findShape(s=>s.type==='text',avatar).characters='S';return {fixture:'Non-owner Sam; owner Alex remains in directory'};
