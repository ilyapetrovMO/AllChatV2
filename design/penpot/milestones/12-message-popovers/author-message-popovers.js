if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const base=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Main / Text channel');if(!base)throw Error('Missing base');
const font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');
function box(parent,name,x,y,w,h,color='#202127',r=8){const s=penpot.createBoard();s.name=name;s.resize(w,h);s.fills=[{fillColor:color,fillOpacity:1}];s.borderRadius=r;parent.appendChild(s);s.x=parent.x+x;s.y=parent.y+y;return s;}
function text(parent,name,copy,x,y,w,size=14,weight=400,color='#F0F1F5'){const t=penpot.createText(copy);t.name=name;font.applyToText(t,font.variants.find(v=>v.fontWeight===String(weight)&&v.fontStyle==='normal'));t.fontSize=String(size);t.resize(w,size*1.4);t.fills=[{fillColor:color,fillOpacity:1}];parent.appendChild(t);t.x=parent.x+x;t.y=parent.y+y;return t;}
function panel(b,name,x,y,w,h){const s=box(b,name,x,y,w,h);s.strokes=[{strokeColor:'#FFFFFF',strokeOpacity:.065,strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];s.shadows=[{style:'drop-shadow',offsetX:0,offsetY:12,blur:32,spread:0,color:{color:'#000000',opacity:.46}}];return s;}
function copyIcon(b,parent,original,name,x,y){const s=penpotUtils.findShape(s=>s.name===original,b).clone();s.name=name;parent.appendChild(s);s.x=parent.x+x;s.y=parent.y+y;return s;}
const keys=['reaction-empty','reaction-custom','mentions','pins-empty','pins-populated'];const emojis=['👍','👎','❤️','😂','🎉','😮','😢','😡','🔥','✨','✅','❌','👀','🙏','💯','🤔','👏','🙌','💪','🚀','⭐','💜','🤣','😭'];const results=[];
for(const [index,key] of keys.entries()){
 const name='Desktop / Main / Message detail / '+key;let b=penpot.currentPage.root.children.find(b=>b.name===name);
 if(!b){const source=key.startsWith('reaction')?penpot.currentPage.root.children.find(b=>b.name==='Desktop / Main / Message / own-actions'):base;if(!source)throw Error('Missing source');b=source.clone();b.name=name;b.x=10400+(index%2)*1400;b.y=Math.floor(index/2)*960;b.showInViewMode=false;for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const i of [...s.interactions])s.removeInteraction(i);const find=n=>penpotUtils.findShape(s=>s.name===n,b),workspace=find('Message workspace');
 if(key.startsWith('reaction')){const p=panel(b,'Reaction picker',784,132,240,206);for(const [i,emoji] of emojis.entries()){const cell=box(p,'Reaction choice '+i,10+(i%6)*38,10+Math.floor(i/6)*38,34,34,i===0?'#30323A':'#202127',5);text(cell,'Emoji '+i,emoji,4,4,28,18.4);cell.setPluginData('reaction',JSON.stringify({emoji,selected:i===0}));}const input=box(p,'Custom reaction input',10,166,182,30,'#202127',5);input.strokes=[{strokeColor:'#FFFFFF',strokeOpacity:.065,strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];text(input,'Custom reaction value',key==='reaction-empty'?'Paste emoji':'🌱',8,5,166,14,400,key==='reaction-empty'?'#727784':'#F0F1F5');const add=box(p,'Add custom reaction',196,166,34,30,'#6D75E8',5);if(key==='reaction-empty')add.opacity=.5;text(add,'Add custom reaction label','+',11,4,20,18);}
 if(key==='mentions'){find('Composer placeholder').characters='@a';const p=panel(b,'Mention suggestions',326,604,360,124);for(const [i,label] of ['Alex','Avery'].entries()){const row=box(p,'Mention option '+i,7,7+i*54,346,54,i===0?'#26282F':'#202127',5);text(row,'Mention name '+i,label,10,8,326,16,700);text(row,'Mention handle '+i,'@'+label.toLowerCase(),10,31,326,13.33,400,'#A4A8B3');row.setPluginData('selected',String(i===0));}}
 if(key.startsWith('pins')){for(const i of key==='pins-empty'?[0,1]:[1])for(const n of ['Message avatar ','Message author ','Message timestamp ','Message body '])find(n+i).remove();const active=box(b,'Pins active button',974,34,36,36,'#30323A',4);copyIcon(b,active,'Pinned messages','Active pin icon',8,8);if(key==='pins-populated')text(workspace,'Pinned indicator','PINNED',80,64,120,11,400,'#F0B232');}
 b.setPluginData('static-detail-state',key);
 }
 results.push({key,id:b.id});
}return results;
