const f=penpot.fonts.findAllByName('Noto Color Emoji')[0];if(!f)throw Error('Emoji font unavailable');const changed=[];for(const b of penpot.currentPage.root.children.filter(b=>b.name.includes('/ Message detail / reaction-'))){for(const t of penpotUtils.findShapes(s=>s.type==='text'&&(s.name.startsWith('Emoji ')||(s.name==='Custom reaction value'&&s.characters==='🌱')),b)){f.applyToText(t,f.variants[0]);changed.push(t.id);}}return {font:f.name,variants:f.variants,changed:changed.length};
