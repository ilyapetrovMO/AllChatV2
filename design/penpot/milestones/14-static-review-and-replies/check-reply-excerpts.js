const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-reply-excerpt'));const checks=[],problems=[];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),quote=shapes.find(s=>s.name==='Reply quote');const expected=b.getPluginData('static-reply-excerpt')==='reply-excerpt'?'Replying to Sam: Thanks for the update.':'Replying to Sam: Message deleted';if(quote?.characters!==expected)problems.push('Wrong quote');if(shapes.some(s=>s.interactions.length))problems.push('Unexpected links');if(shapes.some(s=>s.type==='text'&&!s.textBounds?.width))problems.push('Unrendered text');checks.push({id:b.id,name:b.name,quote:quote?.characters});}const validation=penpot.currentFile.validate();return {pass:boards.length===2&&!problems.length&&!validation.length,problems,checks,validation};
