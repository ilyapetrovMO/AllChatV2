import fs from 'node:fs/promises';import {chromium} from '/home/gosha/src/AllChatV2/node_modules/playwright/index.mjs';
const css=await fs.readFile('/home/gosha/src/AllChatV2/desktop/src/renderer/styles.css','utf8');const b=await chromium.launch({args:['--no-sandbox']});const p=await b.newPage({viewport:{width:1280,height:800}});for(const kind of ['avatar','banner']){await p.setContent(`<style>${css}</style><section class="image-crop-dialog"><h2>Crop ${kind==='avatar'?'Avatar':'Profile Banner'}</h2><div class="image-crop-preview image-crop-${kind}"></div><label>Zoom<input type="range" min="1" max="3" step="0.05" value="1"></label><div class="dialog-actions"><button>Upload ${kind}</button><button>Cancel</button></div></section>`);console.log(kind,await p.locator('.image-crop-dialog').evaluate(el=>[el,...el.querySelectorAll('*')].map(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return {tag:e.tagName,cls:e.className,x:r.x,y:r.y,w:r.width,h:r.height,bg:s.backgroundColor,color:s.color,font:s.font,border:s.border}})));}await b.close();
