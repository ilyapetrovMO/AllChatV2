const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-account-control-state'));const problems=[];for(const b of boards){const key=b.getPluginData('static-account-control-state'),s=penpotUtils.findShapes(()=>true,b);if(s.some(s=>s.interactions.length))problems.push('Interactions');if(key==='username-required'){if(s.some(s=>s.name==='Username value'))problems.push('Username not empty');if(!s.some(s=>s.type==='text'&&s.characters==='Please fill out this field.'))problems.push('Missing validation message');}if(key!=='username-hover'&&!s.some(s=>s.name==='Keyboard focus outline'))problems.push('Missing focus');if(key==='username-hover'&&!s.some(s=>s.name==='Username'&&s.strokes[0]?.strokeOpacity===.14))problems.push('Wrong hover border');}const validation=penpot.currentFile.validate();return {pass:boards.length===4&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
