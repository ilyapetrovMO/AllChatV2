const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-ringtone-control-state')),problems=[];for(const b of boards){const key=b.getPluginData('static-ringtone-control-state'),s=penpotUtils.findShapes(()=>true,b);if(key.includes('minimum')){if(b.width!==960||b.height!==640)problems.push('Wrong minimum size');const pane=s.find(s=>s.name==='Settings viewport');if(pane.width!==648||!pane.clipContent)problems.push('Wrong clipped viewport');const thumb=s.find(s=>s.name==='Ringtone volume thumb');if(key.includes('scrolled')&&(thumb.y+thumb.height>b.y+b.height||thumb.x+thumb.width>b.x+b.width))problems.push('Scrolled volume unreachable');}else if(!s.some(s=>s.name==='Keyboard focus outline'))problems.push('Focus missing');if(s.some(s=>s.interactions.length))problems.push('Interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===6&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
