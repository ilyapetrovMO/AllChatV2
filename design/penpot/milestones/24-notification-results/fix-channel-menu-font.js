const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / notifications-channel-menu');if(!b)throw Error('Missing channel menu');const menu=penpotUtils.findShape(s=>s.name==='Native select menu',b);menu.resize(220,110);const selected=penpotUtils.findShape(s=>s.name==='Selected native option',menu);selected.resize(218,27);const font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');for(const [i,label] of ['Community default','All Messages','Only @mentions','Nothing'].entries()){const old=penpotUtils.findShape(s=>s.name==='Option '+label,menu);const t=penpot.createText(label);t.name=old.name;font.applyToText(t,font.variants.find(v=>v.fontWeight==='400'&&v.fontStyle==='normal'));t.fontSize='16';t.resize(192,22.4);t.fills=old.fills;menu.appendChild(t);t.x=menu.x+14;t.y=menu.y+5+i*27;old.remove();}return {corrected:true};
