# Milestone 25 — Notifications keyboard focus

Five static keyboard-focus references cover Community level, Community mute, notification sound, channel level and channel mute. Source stylesheet specifies a cyan 3px outline offset 2px. Selects additionally use the source purple border and 2px translucent glow; checkboxes retain their checked/unchecked values.

All five native renders visually inspected; native outline/no-interaction checks pass with no file validation issues. Select border/glow refinement also rendered and inspected. Saved/confirmed version is recorded in confirmed-version.json. No new prototype wiring.

Reconstruction: included milestone 23 then author-notification-focus.js on Desktop — Main using the repository MCP helper. Capture/review scripts require repository MCP SDK/Playwright and milestone 03 Inter data. IDs, native SVGs/PNGs, checks and save records included. This is a reconstruction backup, not a native .penpot export/reimport.

Notifications hover/minimum-window references and exact runtime comparison remain pending, as do other desktop families and the native file-picker investigation.
