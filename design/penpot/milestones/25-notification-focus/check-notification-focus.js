const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-notification-focus-state')),problems=[];for(const b of boards){const s=penpotUtils.findShapes(()=>true,b),o=s.filter(s=>s.name==='Keyboard focus outline');if(o.length!==1||o[0].strokes[0]?.strokeWidth!==3)problems.push('Wrong outline');if(s.some(s=>s.interactions.length))problems.push('Interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===5&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
