# Milestone 26 — Notifications minimum window and hover

Four static references: 960×640 initial and scrolled layouts, Community level hover, and channel level hover. Source CSS measured locally with embedded Inter shows the description wrapping to two lines, scrollHeight 758 versus viewport height 564, and 194px maximum scroll in the single-channel fixture. The static scrolled view exposes the channel selector and mute control. Select hover uses white border at 18% opacity.

Native board/viewport dimensions, wrapped copy, scrolled control bounds, hover borders and no-interaction checks pass, with no file validation issues. All four native SVG renders visually inspected. Saved/confirmed version recorded separately. No new prototype or production code change. The local measurement DOM is a disposable CSS reference only.

Reconstruction: included milestone 23 then author-notification-layouts.js on Desktop — Main using the repository MCP helper. Repository MCP SDK, Playwright and milestone 03 Inter data support scripts. Local measurements/screenshots, native SVG/PNG designs, identifiers, checks and save evidence are included. This is a reconstruction backup, not a native .penpot export/reimport.

Geometry retains small manual approximations from the earlier base. Exact runtime/header typography comparison, long-channel/multi-row minimum variants, cross-platform menus and broader desktop coverage remain open. The native file-picker investigation remains unresolved.
