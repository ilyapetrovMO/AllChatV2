const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-settings-state'));for(const b of boards){for(const name of ['Speaker test','Reset Voice & Video settings']){const label=penpotUtils.findShape(s=>s.name===name+' label',b);label.getRange(0,label.characters.length).fontSize='16';label.resize(540,22.4);const detail=penpotUtils.findShape(s=>s.name===name+' description',b);detail.getRange(0,detail.characters.length).fontSize='13.33';detail.resize(600,19);}const output=penpotUtils.findShape(s=>s.name==='Input sensitivity output',b);output.resize(56,18.2);}return {refined:boards.length};
