const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-feedback-text-reference');if(b)b.remove();for(const board of penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-feedback-state'))){const t=penpotUtils.findShape(s=>s.name==='Noise suppression value',board);if(t.characters==='Enhanced (RNNoise)')t.growType='auto-width';}return {referenceRemoved:!!b};
