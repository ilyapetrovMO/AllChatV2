const stage=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-feedback-text-reference');if(!stage)throw Error('Missing text reference');const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-feedback-state')),result=[];
for(const b of boards){const key=b.getPluginData('static-voice-feedback-state'),template=stage.children.find(s=>s.name===key);if(!template?.textBounds?.width)throw Error('Unrendered template '+key);const old=penpotUtils.findShape(s=>s.name==='Voice settings notice',b),t=template.clone();old.parent.appendChild(t);t.name='Voice settings notice';t.x=old.x;t.y=old.y;old.remove();result.push({key,id:t.id,bounds:t.textBounds});}return result;
