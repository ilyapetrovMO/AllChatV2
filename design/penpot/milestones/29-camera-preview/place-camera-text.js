const stage=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / camera-text-reference');if(!stage)throw Error('Missing native text reference');const results=[];
for(const b of penpot.currentPage.root.children.filter(b=>b.getPluginData('static-camera-preview-state'))){for(const name of ['Stop Video label','Voice settings notice']){const template=stage.children.find(s=>s.name===name);if(!template.textBounds?.width)throw Error('Unrendered reference '+name);const old=penpotUtils.findShape(s=>s.name===name,b),t=template.clone();old.parent.appendChild(t);t.name=name;t.x=old.x;t.y=old.y;if(name==='Stop Video label')t.x=t.parent.x+(t.parent.width-t.textBounds.width)/2;old.remove();}results.push({id:b.id});}stage.remove();return results;
