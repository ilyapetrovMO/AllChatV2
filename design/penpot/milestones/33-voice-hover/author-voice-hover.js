if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-default');if(!source)throw Error('Missing voice base');const results=[];for(const [i,key] of ['voice-hover-selector','voice-hover-range','voice-hover-button'].entries()){let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+key);if(!b){b=source.clone();b.name='Desktop / Settings / '+key;b.x=88000+i*1400;b.y=0;b.setPluginData('static-voice-settings-state','');b.showInViewMode=false;const find=n=>penpotUtils.findShape(s=>s.name===n,b);if(key.endsWith('selector'))find('Microphone device').strokes=[{strokeColor:'#FFFFFF',strokeOpacity:.18,strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];if(key.endsWith('button'))find('Mic Test button').fills=[{fillColor:'#7B7B7B',fillOpacity:1}];if(key.endsWith('range')){const track=find('Microphone volume track'),fill=find('Microphone volume fill'),thumb=find('Microphone volume thumb');track.y-=1;track.resize(220,8);track.borderRadius=4;track.fills=[{fillColor:'#E5E5E5',fillOpacity:1}];track.strokes=[{strokeColor:'#9A9A9A',strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];fill.y=track.y;fill.resize(110,8);fill.borderRadius=4;fill.fills=[{fillColor:'#3D47E0',fillOpacity:1}];fill.strokes=[{strokeColor:'#464B98',strokeWidth:1,strokeStyle:'solid',strokeAlignment:'inner'}];thumb.fills=[{fillColor:'#3D47E0',fillOpacity:1}];}b.setPluginData('static-voice-hover-state',key);for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}results.push({key,id:b.id});}return results;
