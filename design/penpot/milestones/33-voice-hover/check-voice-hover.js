const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-hover-state')),problems=[];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),find=n=>shapes.find(s=>s.name===n),key=b.getPluginData('static-voice-hover-state');if(key.endsWith('selector')&&find('Microphone device').strokes[0].strokeOpacity!==.18)problems.push('Selector border');if(key.endsWith('button')&&find('Mic Test button').fills[0].fillColor.toUpperCase()!=='#7B7B7B')problems.push('Button color');if(key.endsWith('range')){for(const name of ['Microphone volume fill','Microphone volume thumb'])if(find(name).fills[0].fillColor.toUpperCase()!=='#3D47E0')problems.push(name);if(Math.abs(find('Microphone volume track').width-220)>.001||Math.abs(find('Microphone volume track').height-8)>.001)problems.push('Range bounds');}if(shapes.some(s=>s.name==='Keyboard focus outline'||s.interactions.length))problems.push('Unexpected focus or interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===3&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
