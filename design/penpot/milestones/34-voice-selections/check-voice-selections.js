const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-selection-state')),problems=[];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),control=shapes.find(s=>s.name===b.getPluginData('voice-selection-control')),value=shapes.find(s=>s.name===control.name+' value'),expected=b.getPluginData('voice-selection-value');if(!penpot.generateMarkup([value],{type:'svg'}).includes('font-weight:700'))problems.push(b.name+': exported font weight');if(value.characters!==expected||!penpot.generateMarkup([value],{type:'svg'}).includes(expected))problems.push(b.name+': exported value');if(!value.textBounds?.width||value.textBounds.x+value.textBounds.width>control.x+control.width-20)problems.push(b.name+': label clipping');if(control.y<b.y+76||control.y+control.height>b.y+b.height)problems.push(b.name+': selector hidden');if(shapes.some(s=>s.name==='Native voice select menu'||s.name==='Keyboard focus outline'||s.interactions.length))problems.push(b.name+': unexpected overlay or interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===6&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
