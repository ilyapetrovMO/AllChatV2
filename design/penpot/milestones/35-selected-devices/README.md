# Milestone 35 — Selected devices

Three editable static references show Microphone 1, Speaker 1 and Camera 1 selected in their closed controls. The fixtures use the implementation's numbered fallback for devices without names; no hardware enumeration or media capture was performed. Each changes one selection and retains idle tests and camera-off state. No prototype interactions or production code changes.

Verified native menu labels were reused, styled bold and refreshed near the canvas origin before final placement. All three native SVG/PNG views were visually reviewed. Checks verify actual exported values/weight, label bounds, visible selectors and absence of menu overlays/interactions. File validation has no issues. Named save and independent confirmation are included.

Reconstruction: restore included milestone 30 and prerequisites. Run author-selected-devices.js, settle-selected-devices.js, then refresh-away.js and refresh-back.js in separate MCP calls. Run check-selected-devices.js, finish-selected-devices.js and review final exports. Resolve refresh page IDs by name in a reconstructed file. Repository MCP SDK, Playwright and milestone 03 Inter support the helpers.

This reconstruction backup includes prerequisites, scripts, native SVG/PNG, IDs, checks and save evidence; it is not a native .penpot export or verified reimport. Named/long-device fixtures, volume ranges, minimum-window and full runtime/header/font fidelity, and broader desktop coverage remain open.
