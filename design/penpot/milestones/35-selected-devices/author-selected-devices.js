if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const specs=[['voice-device-microphone-selected','voice-default','microphone','Microphone device','Microphone 1'],['voice-device-speaker-selected','voice-default','speaker','Speaker device','Speaker 1'],['voice-device-camera-selected','voice-camera','camera','Camera device','Camera 1']],results=[];let created=0;
for(const [i,[key,baseKey,menuKey,controlName,value]] of specs.entries()){let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+key);if(!b&&created<3){const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+baseKey),menu=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-menu-'+menuKey);if(!source||!menu)throw Error('Missing prerequisite');const template=penpotUtils.findShape(s=>s.name==='Menu option '+value,menu);if(!template.textBounds?.width)throw Error('Unrendered option');b=source.clone();b.name='Desktop / Settings / '+key;b.x=103000+i*1400;b.y=0;b.showInViewMode=false;b.setPluginData('static-voice-settings-state','');const old=penpotUtils.findShape(s=>s.name===controlName+' value',b),t=template.clone();old.parent.appendChild(t);t.name=old.name;t.x=old.x;t.y=old.y;t.resize(old.width,18.2);const range=t.getRange(0,t.characters.length);range.fontSize='13';range.fontWeight='700';range.fills=[{fillColor:'#F0F1F5',fillOpacity:1}];old.remove();b.setPluginData('static-selected-device-state',key);b.setPluginData('voice-selection-control',controlName);b.setPluginData('voice-selection-value',value);for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);created++;}if(b)results.push({key,id:b.id,value});}return {created,remaining:specs.length-results.length,boards:results};
