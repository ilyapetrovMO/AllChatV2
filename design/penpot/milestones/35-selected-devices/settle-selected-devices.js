const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-selected-device-state'));for(const b of boards){b.x=100;b.y=100;const value=penpotUtils.findShape(s=>s.name===b.getPluginData('voice-selection-control')+' value',b);value.growType='auto-height';value.resize(value.parent.width-38,18.2);value.getRange(0,value.characters.length).fontWeight='700';value.getRange(0,value.characters.length).fontSize='13';}penpot.viewport.zoomIntoView(boards);return {settled:boards.length};
