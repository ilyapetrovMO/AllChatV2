if(penpot.currentPage.name!=='Desktop — Main')throw Error('Expected main page');const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-default');if(!source)throw Error('Missing voice base');const states=[['voice-volume-microphone-min','Microphone volume',0,0],['voice-volume-microphone-max','Microphone volume',200,1],['voice-volume-speaker-min','Speaker volume',0,0],['voice-volume-speaker-half','Speaker volume',50,.5]],results=[];
for(const [i,[key,control,value,fraction]] of states.entries()){let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+key);if(!b){b=source.clone();b.name='Desktop / Settings / '+key;b.x=108000+i*1400;b.y=0;b.showInViewMode=false;b.setPluginData('static-voice-settings-state','');const find=n=>penpotUtils.findShape(s=>s.name===n,b),output=find(control+' output'),track=find(control+' track'),fill=find(control+' fill'),thumb=find(control+' thumb');output.characters=value+'%';if(fraction===0)fill.remove();else fill.resize(212*fraction,6);thumb.x=track.x+204*fraction;b.setPluginData('static-voice-volume-state',key);b.setPluginData('volume-control',control);b.setPluginData('volume-value',String(value));b.setPluginData('volume-fraction',String(fraction));for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}results.push({key,id:b.id,value});}return results;
