# Milestone 37 — Voice & Video minimum window

Four editable static 960×640 views show the initial form, Input processing, Camera and the bottom sections. Source window policy establishes this minimum. A static fixture extracted from the actual VoiceVideoSettings JSX preserves the five selectors, three ranges, three checkboxes and four buttons, with exact defaults and range limits and no event handlers. It is a disposable measurement fixture, not a product prototype.

Source CSS with the existing embedded Inter assets measures 584px content width, 1799.52px height and 1344px maximum scroll. Cards and text/control positions are mapped from DOM bounds. The introduction wraps to two lines, the suppression/quality descriptions to three, and the speaker-test description to two. All four final native SVG/PNG views were visually reviewed; processing was directly compared with its corresponding local source capture. Checks verify viewport dimensions, text bounds/wrapping, button centering, lack of suppression overlap, no interactions and a fully visible reference for every one of 16 control/preview targets across the four views. File validation has no issues. Named save and independent confirmation are included.

## Reconstruction

Restore included milestone 27. Run author-voice-minimum.js once for the measured initial view. Run stage-minimum-text.js in three bounded batches to prepare 43 fresh text layers, export that helper once, then position-minimum-text.js. Refresh away/back in separate MCP calls and run place-minimum-text.js only when its bounds guard passes. It installs the text and removes the helper. Run author-voice-minimum.js again to create the scroll views. Run refine-voice-minimum.js, refresh away/back, then finish-voice-minimum.js, check-voice-minimum.js and inspect all four exports. Resolve refresh page IDs by name in a reconstructed file. The author embeds the recorded measurement table; extraction/measurement scripts can regenerate the source evidence. Repository MCP SDK, Playwright and milestone 03 Inter support helpers.

Two large requests returned gateway timeouts after their edits had completed. Live inspection confirmed the completed minimum board and later removal of the text helper; those mutations were not blindly repeated. diagnostics/ contains intermediate stale-text inspection data and an overlapping staging SVG, not the final designs. Final views are in frames/.

Fresh native text repaired stale line wrapping and centering. The narrow suppression label and sensitivity output use single-line sizing; the suppression label uses Inter 800 to match the supplied measurement font assets' closest weight for CSS bolder. Native text metrics otherwise retain small differences from browser layout. Native checkbox glyphs, slider paint, inherited header and cross-platform/system typography are not claimed pixel-identical.

This reconstruction backup contains prerequisites, scripts, source fixture/captures/measurements, native SVG/PNG, IDs, checks and save evidence. It is not a native .penpot export or verified reimport. Named/long-device, minimum camera-on/menu variants and broader desktop design coverage remain open. No prototype interactions or production code changes were added.
