{
  "source": "desktop/src/renderer/app.tsx:VoiceVideoSettings",
  "preferences": {
    "microphoneID": "",
    "speakerID": "",
    "cameraID": "",
    "inputGain": 1,
    "outputVolume": 1,
    "noiseSuppressionMode": "standard",
    "echoCancellation": true,
    "autoGainControl": false,
    "noiseGate": true,
    "noiseGateThresholdDB": -50,
    "screenShareMode": "auto"
  },
  "replacedExpressions": [
    "preferences.microphoneID",
    "(event) => patch({ microphoneID: event.target.value })",
    "microphones.map((device, index) => <option key={device.deviceId} value={device.deviceId}>{device.label || `Microphone ${index + 1}`}</option>)",
    "preferences.speakerID",
    "(event) => patch({ speakerID: event.target.value })",
    "speakers.map((device, index) => <option key={device.deviceId} value={device.deviceId}>{device.label || `Speaker ${index + 1}`}</option>)",
    "Math.round(preferences.inputGain * 100)",
    "preferences.inputGain",
    "(event) => patch({ inputGain: Number(event.target.value) })",
    "Math.round(preferences.outputVolume * 100)",
    "preferences.outputVolume",
    "(event) => patch({ outputVolume: Number(event.target.value) })",
    "() => void testMicrophone()",
    "preferences.noiseSuppressionMode",
    "(event) => patch({ noiseSuppressionMode: event.target.value as DesktopVoicePreferences[\"noiseSuppressionMode\"] })",
    "preferences.echoCancellation",
    "(event) => patch({ echoCancellation: event.target.checked })",
    "preferences.autoGainControl",
    "(event) => patch({ autoGainControl: event.target.checked })",
    "preferences.noiseGate",
    "(event) => patch({ noiseGate: event.target.checked })",
    "preferences.noiseGateThresholdDB",
    "preferences.noiseGateThresholdDB",
    "(event) => patch({ noiseGateThresholdDB: Number(event.target.value) })",
    "cameraPreview",
    "!cameraStream",
    "Boolean(cameraStream)",
    "preferences.cameraID",
    "(event) => patch({ cameraID: event.target.value })",
    "cameras.map((device, index) => <option key={device.deviceId} value={device.deviceId}>{device.label || `Camera ${index + 1}`}</option>)",
    "() => void toggleCamera()",
    "cameraStream ? \"Stop Video\" : \"Test Video\"",
    "preferences.screenShareMode",
    "(event) => patch({ screenShareMode: event.target.value as DesktopVoicePreferences[\"screenShareMode\"] })",
    "playSpeakerTest",
    "() => { save({ ...defaultDesktopVoicePreferences, ringtoneVolume: preferences.ringtoneVolume }); setNotice(\"Voice & Video settings were reset.\"); }",
    "notice"
  ]
}