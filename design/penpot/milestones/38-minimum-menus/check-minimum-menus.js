const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-minimum-menu-state')),problems=[];for(const b of boards){const shapes=penpotUtils.findShapes(()=>true,b),menu=shapes.find(s=>s.name==='Native voice select menu'),control=shapes.find(s=>s.name===b.getPluginData('menu-control')),options=JSON.parse(b.getPluginData('menu-options'));if(b.width!==960||b.height!==640||Math.abs(menu.width-control.width)>.001||Math.abs(menu.y-control.y-control.height)>.001)problems.push(b.name+': menu geometry');if(menu.x<b.x||menu.x+menu.width>b.x+960.01||menu.y<b.y+76||menu.y+menu.height>b.y+640.01)problems.push(b.name+': menu clipped');for(const label of options){const t=shapes.find(s=>s.name==='Menu option '+label);if(!t||!penpot.generateMarkup([t],{type:'svg'}).includes(label)||t.textBounds.width>menu.width-20)problems.push(b.name+': option '+label);}if(shapes.some(s=>s.interactions.length))problems.push(b.name+': interaction');}const validation=penpot.currentFile.validate();return {pass:boards.length===5&&!problems.length&&!validation.length,problems,validation,revision:penpot.currentFile.revn};
