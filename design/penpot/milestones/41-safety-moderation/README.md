# Milestone 41 — Safety reports and moderator controls

Six editable static views show Member reports (top/bottom), moderator form (top/middle/bottom) and expanded Moderation Records. Synthetic open/resolved reports illustrate permission differences; Resolve appears only for moderators. The form contains Action (Warn), Member (Alex), Reason, Duration in minutes, Apply Action, records disclosure and Purge Old Records. Expanded records show a synthetic warning reason/outcome. Account export/deletion controls follow below.

Source-derived HTML/CSS fixtures measured in local Chromium establish all body geometry and scroll positions. Collapsed details descendants are explicitly excluded from measurements even though browsers can expose their layout rectangles. Native SVG/PNG views were visually inspected. Checks cover zero interactions, text rendering, permission differences, collapsed/expanded visibility and visible control references. Named save and independent confirmation included.

Reconstruction: restore included milestone 40 and its prerequisites. Run author-safety-moderation.js until remaining is zero (three bounded batches). Export the initial views to settle text. Run finish-safety-moderation.js until remaining and unpositioned are both zero (six bounded steps), then check-safety-moderation.js and inspect final native exports. Measurement and rendering use repository SDK/Playwright helpers and milestone 03 fonts. Two authoring gateway timeouts were resolved by waiting the service retry interval and inspecting live state; both edits had completed. No duplicate authoring was performed. No reports/moderation actions were sent.

This is a reconstruction backup, not a native .penpot export or verified reimport. Native arrow/resize affordances, inherited header and exact platform typography remain approximate. Menus, validation, native dialogs, action feedback, input variants, minimum window and broader desktop design coverage remain open. No prototype or production code changes.
