if(penpot.currentPage.name!=='Desktop — Main')throw Error('Wrong page');const key='safety-delete-confirm';let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / '+key);if(!b){const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / safety-filled-delete'),ref=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / sessions-revoke-confirm');if(!source||!ref)throw Error('Missing prerequisite');b=source.clone();b.name='Desktop / Settings / '+key;b.x=0;b.y=0;b.showInViewMode=false;b.setPluginData('static-safety-input-state','');const dialog=penpotUtils.findShape(s=>s.name==='Native Linux confirmation reference',ref).clone();b.appendChild(dialog);dialog.resize(320,81);dialog.x=b.x+480;dialog.y=b.y+359.5;penpotUtils.findShape(s=>s.name==='Confirmation question',dialog).remove();for(const [label,x] of [['Ok',0],['Cancel',160]]){const btn=penpotUtils.findShape(s=>s.name===label+' native button',dialog);btn.resize(160,34);btn.x=dialog.x+x;const t=penpotUtils.findShape(s=>s.name===label+' native label',btn);t.x=btn.x+(160-t.textBounds.width)/2;}const font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter'),t=penpot.createText('Permanently anonymize this Account?');t.name='Confirmation question';font.applyToText(t,font.variants.find(v=>v.fontWeight==='400'&&v.fontStyle==='normal'));t.fontSize='13';t.resize(280,18.2);t.fills=[{fillColor:'#303638',fillOpacity:1}];dialog.appendChild(t);t.x=dialog.x+35;t.y=dialog.y+7;b.setPluginData('static-safety-dialog-state',key);b.setPluginData('native-dialog-limits','Linux Electron 43.4.0 confirmation content measured in Xvfb without window manager. Centre placement illustrative; OS decoration/placement and exact platform typography remain pending.');for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}return [{key,id:b.id}];
