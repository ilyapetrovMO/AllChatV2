const stage=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / safety-dialog-text-reference');if(!stage)throw Error('Missing reference');const b=penpot.currentPage.root.children.find(b=>b.getPluginData('static-safety-dialog-state')==='safety-delete-confirm'),d=penpotUtils.findShape(s=>s.name==='Native Linux confirmation reference',b),source=stage.children[0];if(!source.textBounds?.width)throw Error('Unrendered reference');const t=source.clone();d.appendChild(t);t.resize(280,24);t.x=d.x+35;t.y=d.y+7;stage.remove();b.x=167000;b.y=0;return {id:b.id};
