# Milestone 47 — Minimum-window Safety menus

Three editable static 960×640 views show Report a Member, moderation Action, and moderation Member dropdowns. Local source-derived Chromium captures confirm 584px menus matching their controls, opening below each selector. The report menu uses the top view; moderation menus use scroll 640px.

Nine option rows reuse verified native text, selected-row and focus styling from the wider references. All three final native SVG/PNG exports visually reviewed against source captures. Checks cover minimum dimensions, popup alignment, option copy/rendering, clipping, zero interactions and file validation. Named save independently confirmed.

Reconstruction: restore included milestones 42 and 46 and their prerequisites, run author-safety-small-menus.js until remaining is zero (three batches), run check-safety-small-menus.js and inspect exports. Measurement/rendering helpers use repository MCP/Playwright and milestone 03 fonts. Member names are synthetic; no reports or moderation actions were sent.

This reconstruction backup is not a native .penpot export or verified reimport. Minimum-window validation/input variants, long/empty Member fixtures, full native-control/header/platform typography and broader desktop coverage remain unfinished. No prototypes or production changes.
