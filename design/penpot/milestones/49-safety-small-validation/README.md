# Milestone 49 — Minimum-window Safety validation

Eight editable static 960×640 views cover required report Member/reason, short report reason, required moderation reason, negative duration, required deletion password/confirmation and confirmation pattern mismatch. Synthetic inputs and prevented submissions reproduce source native validation locally without sending actions.

Fresh source captures and pixel border measurements establish minimum-window bubble geometry. The long short-text message narrows from 637px to 477px and wraps before currently. Other messages retain their native widths while moving with their fields. All eight native SVG/PNG references visually reviewed; checks cover message copy, rendering, focus alignment, field values, popup containment, dimensions, file validation and no interactions.

Reconstruction: restore included milestones 44 and 46 and their prerequisites, run author-safety-small-validation.js until remaining is zero (eight batches), export to settle the new long message, run finish-safety-small-validation.js, check-safety-small-validation.js and inspect final exports. Local recapture uses measure-safety-small-validation.mjs followed by measure-small-validation-bubbles.py. Repository MCP/Playwright/Pillow and milestone 03 fonts support the tools.

This reconstruction backup is not a native .penpot export or verified reimport. Native bubble typography/pointer/shadow and inherited shell remain approximate across platforms. Other edge cases and broader desktop coverage remain unfinished. No prototype or production changes.
