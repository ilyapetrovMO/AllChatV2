# Milestone 51 — Community General entry

Two editable 1280×800 static boards cover Community General loading and generic load failure. Each includes the six administration tabs, selected General tab, Community search, retained desktop shell and Member panel. Entry context is Community Home without an active conversation or call. Other contextual header variants remain open.

The source-derived local HTML/CSS fixture uses synthetic data and standards-mode Chromium with embedded Inter. Body and navigation text/control positions are measured from the fixture. Native SVG/PNG exports were visually reviewed; checks verify dimensions, six navigation controls, twelve text positions, zero prototype interactions and file validation. Named save independently confirmed at revision 82.

Reconstruction: restore included milestone 07 to create the main base. Execute author-community-general-entry.js twice (it skips existing named boards), then finish-community-general-entry.js, and check-community-general-entry.js. Export and visually inspect both boards. Helpers require repository MCP/Playwright and milestone 03 embedded fonts. No screenshot upload is required.

This is a reconstruction backup, not a native .penpot export or verified reimport. Inherited shell/header icons and platform typography remain approximations; source fixtures are not live authenticated app captures. Server-provided errors may differ. Loaded General form, avatar/ringtone states, validation, minimum window, search/contextual headers and broader administration coverage remain unfinished. No prototypes or production changes were made.
