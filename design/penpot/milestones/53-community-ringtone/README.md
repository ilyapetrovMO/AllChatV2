# Milestone 53 — Community ringtone

Ten editable static references cover custom audio, saved upload, failed upload with generated audio, failed upload with existing custom audio, and successful removal. Each has top and bottom scroll views at1280×800. Source retains the chosen filename after removal; removal notice says Using the generated tone. A failed upload preserves the previously active ringtone. Synthetic ringtone.ogg is a local fixture, not uploaded audio.

Local source-derived standards-mode Chromium fixtures establish the varying card heights and scroll positions. Native exports visually reviewed; text bounds, control visibility across views, zero interactions and file validation checked. Named save independently confirmed; see confirmed-version.json. The earlier General form is retained and shifted with each measured card height.

Reconstruction: restore included milestone52 and prerequisites. Run author-community-ringtone.js until remaining0, then finish-community-ringtone.js until remaining0, and once more to return the board manifest. Run check-community-ringtone.js and inspect exported boards. Helpers need repository MCP/Playwright and milestone03 embedded fonts.

Reconstruction backup, not native .penpot export or verified reimport. Native button bevels, file selector details and platform fonts remain approximations. Source captures are static fixtures, not live authenticated app sessions. File picker, pending upload timing, other file/notice edge cases, avatar states, form failures/validation, minimum window and broader desktop coverage remain open. No prototype, production edit or live audio upload.
