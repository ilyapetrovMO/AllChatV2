const stage=penpot.currentPage.root.children.find(b=>b.name==='Avatar text rendering stage');if(!stage)throw Error('Missing rendering stage');const out=[];for(const t of [...stage.children]){const c=penpot.currentPage.getShapeById(t.getPluginData('avatar-original-parent')),m=JSON.parse(t.getPluginData('general-loaded-target')),n=t.clone();n.resize(150,24);c.appendChild(n);n.x+=c.x+m.x-n.textBounds.x;n.y+=c.y+m.y-n.textBounds.y;out.push({id:n.id,name:n.name,width:n.textBounds.width});}stage.remove();return out;
