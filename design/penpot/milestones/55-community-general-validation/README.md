# Milestone 55 — General native validation

Seven editable static1280×800 references cover required Community name, required attachment limit, limits below1/above256, fractional limit1.5, invalid raw number e, and malformed relay URL. Values, exact native messages, focus, numeric spinner, clicked Save appearance and popup placement follow local source-derived Chromium captures. No settings update is sent.

The source form requires integer attachment limits1–256; the name is required with maxlength100 and the optional relay field has typeURL. The browser retains raw e visually while exposing an empty DOM value and badInput. Every captured error keeps scroll531. Bubbles are measured from settled local pixels; native shapes and Inter text remain editable approximations of platform controls.

Native exports visually reviewed; messages/values, focused-field and popup bounds, text fit, spinner visibility, no interactions and file validation checked. Named save independently confirmed; see confirmed-version.json.

Reconstruction: restore included milestone52 and prerequisites, run author-community-general-validation.js seven times, then finish-community-general-validation.js, repair-general-validation-text.js, place-general-validation-text.js and check-community-general-validation.js. Export and inspect all seven boards. Source capture helpers need repository Playwright/MCP and milestone03 Inter fonts; bubble measurement uses Pillow. No screenshot upload is required.

Reconstruction backup, not native .penpot export or verified reimport. Exact platform fonts/native bevels, caret timing, minimum window, valid input boundaries, server-side save failure, other Community settings states and broader desktop coverage remain open. No prototypes or production edits.
