# Community administration static coverage

Source: desktop/src/renderer/app.tsx CommunityAdministration and desktop/src/renderer/styles.css. Static designs only. This is an open audit, not an exhaustive coverage claim.

Milestone 51 covers General loading and generic load-failure states at 1280×800, with six navigation tabs and Community search. Source-derived local fixtures use synthetic AllChat Community data and standards-mode Chromium. Entry context is Community Home with no active conversation or call. Header contextual actions when settings is opened from a conversation remain to be audited.

Remaining General coverage: loaded form, Community avatar absent/present/upload/remove states, generated/custom ringtone and controls, all editable fields, expanded relay identity, save failure, native validation, minimum window and scroll views, long content, search/filter/results variants. Server-provided error text can differ from the generic example. No dedicated retry button exists; selecting General requests settings again when settings are absent.

Other administration sections remain open: Dashboard loading/error/loaded health and charts; Channels creation/archive/permissions; Roles creation/edit/reorder/delete; Invitations creation/list/revoke; Soundboard loading/empty/upload/limits/edit/delete. Enumerate their controls and states from source before claiming coverage. Native prompt support needs checking where used.

Exact platform fonts, inherited shell/header fidelity and component/token propagation remain unverified. Local source captures are reference fixtures, not end-to-end application screenshots.

Milestone 52: source-derived loaded General fixtures cover the default avatar initial, generated ringtone, Community name, attachment limit, Guide, empty push relay, collapsed/expanded relay identity and Save settings. Source content origin is (344,116), width904; the avatar top margin shifts the content origin below the usual 104px. Default height1134.734375, expanded1280.6875; maximum scroll531/677. Values and public-key text are explicitly synthetic. Six native static top/middle/bottom views visually reviewed. Eight default controls and nine expanded controls have fully visible references; text positions and no-interaction/file validation checks pass. File selector geometry, native Save button bevel and monospace key-ID typography remain fidelity gaps. Avatar/ringtone variants, save failures, validation, minimum window and broader administration coverage remain open.

Milestone53: five Community ringtone states measured from source: existing custom (no selected file), saved (custom active), failed upload (generated retained), failed upload (custom retained), removed (generated active, selected filename retained). Selected file is synthetic ringtone.ogg. Custom card height288.890625; saved/custom-error311.28125; generated-error/reset270.890625. Maximum scroll571/594/553 respectively. Ringtone notice is not reset before another upload, so pending/repeated-upload combinations need separate consideration. Removal has no catch or dedicated failure notice in the source. Native file picker, pending file selection, other file/content edge cases and minimum-window variants remain open. Native validation, avatar coverage and broader desktop work are also unfinished.

Milestone53 native review: all ten top/bottom exports visually checked. Active audio, exact notice copy, selected filename, text bounds and complete control visibility across each pair pass; no prototype interactions and no file validation errors. Reconstruction backup accompanies the named milestone save. Native bevel/file-selector/platform font fidelity remains open.

Milestone54: Community avatar initial-load fallback with an avatar URL, loaded image, completed upload with retained filename, and successful removal with retained filename. The no-avatar initial is already in milestone52. All four source fixtures retain the same content height and lower form layout as milestone52, so its middle/bottom references remain applicable. Synthetic blue/white/orange avatar artwork is code-drawn locally and reconstructed as editable native shapes; no private image is uploaded.

AuthenticatedImage initially renders the supplied initial until load_asset returns an asset. A rejected initial load or non-asset result retains the initial without a dedicated error message. This does not cover image decode failure after a blob URL is set, which has no onError fallback. Changing the path does not clear the previous source immediately, so replacement-pending can retain the old image. Avatar upload/removal have no dedicated notice or caught failure state. Native file picker, pending replacement, decode error, filename/content extremes, minimum window and platform fidelity remain open.

Milestone54 native review: four exports visually checked after remounting five initially unrendered labels. Image/initial, removal control, filename, text position, unchanged content height, no-interaction and file validation checks pass. Lower form references from milestone52 remain applicable. Named save and reconstruction backup accompany this milestone.

Milestone55: General native validation captured for empty name, empty attachment limit, limits0/257, fractional1.5, nonnumeric raw e, and malformed relay URL. The attachment field requires integers1–256 via min/max/default step; malformed number retains displayed e while DOM value is empty and badInput true. All captures are at scroll531 with failed field focused. Source maximum name length100 is enforced during entry rather than a separate overlength native tooltip. Server-side save failure and input-boundary/valid-value fixtures remain open.

Validation fixtures use standards-mode Chromium and local synthetic values. Popup bounds were measured from settled screenshots, including rounded horizontal border endpoints extended by5px to the vertical sides. Focus styling, numeric spinner and clicked Save appearance are captured; native popup glyphs/bevels remain platform-dependent. No live settings update occurs.

Milestone55 native review: all seven exports visually checked after remounting twelve message/value labels. Exact messages and entered values, focus/popup geometry, rendered text fit, spinner visibility, no interactions and file validation pass. Named save and reconstruction backup accompany this milestone. Native font/bevel and other desktop fidelity remain open.
