if(penpot.currentPage.name!=='Desktop — Main')throw Error('Wrong page');const out=[];for(const [i,key]of ['loading','error'].entries()){let b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / general-small-'+key);if(!b){const source=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / community-general-'+key);if(!source)throw Error('Missing '+key);b=source.clone();b.name='Desktop / Settings / general-small-'+key;b.x=278000+i*1100;b.y=0;b.resize(960,640);b.setPluginData('static-community-general-state','');const find=n=>penpotUtils.findShape(s=>s.name===n,b);find('Desktop title bar').resize(960,28);for(const n of ['Window minimize','Window maximize','Window close'])find(n).x-=320;find('Community rail').resize(72,612);find('Conversation sidebar').resize(240,612);for(const n of ['Current member panel','Add community','Add icon'])find(n).y-=160;find('Channel header').resize(648,48);find('Search field').x-=320;find('General settings viewport').resize(648,564);b.setPluginData('static-general-minimum-entry',key);b.showInViewMode=false;for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);}out.push({key:'general-small-'+key,id:b.id});}return out;
