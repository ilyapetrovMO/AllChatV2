# Milestone 60 — Minimum-window Community avatar

Four editable960×640 static views cover loaded avatar image, initial-load fallback with avatar URL, completed upload with retained avatar.png filename, and successful removal with retained filename. The source flex row shrinks the file input to213.25px and wraps Remove avatar into two centered lines in a94.75×50.78125px button. The button remains visible. Removed state restores306px file-input width.

Source-derived local standards-mode Chromium captures establish geometry and copy. Row height64 and total General content height1179.515625 are unchanged; milestone58 lower-scroll form references remain applicable. Synthetic artwork and mature labels are reused from milestone54; no private image upload or live avatar action occurs.

Four native exports visually reviewed. Checks cover image/initial states, filename retention, input widths, wrapped removal-label copy/height/fit, text positions, content size, no interactions and file validation. Named save independently confirmed; see confirmed-version.json.

Reconstruction: restore included milestones58 and54 with prerequisites. Run author-avatar-minimum.js four times, finish-avatar-minimum.js and check-avatar-minimum.js. If native text positions settle later, rerun the finish script before checking/exporting. Helpers require repository MCP/Playwright and milestone03 Inter fonts.

Reconstruction backup, not native .penpot export or verified reimport. Native bevels/platform font fidelity, image decode/pending/file-picker/long-name edge cases, narrow ringtone/validation/input variants and broader desktop coverage remain open. No prototypes or production edits.
