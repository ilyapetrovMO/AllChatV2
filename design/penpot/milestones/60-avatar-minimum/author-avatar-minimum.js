if(penpot.currentPage.name!=='Desktop — Main')throw Error('Wrong page');const keys=['image','fallback','uploaded','removed'],key=keys.find(k=>!penpot.currentPage.root.children.some(b=>b.name==='Desktop / Settings / avatar-small-'+k));if(!key)return {remaining:0};const base=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / general-small-default'),wide=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / community-avatar-'+key);if(!base||!wide)throw Error('Missing saved base');const b=base.clone();b.name='Desktop / Settings / avatar-small-'+key;b.x=keys.indexOf(key)*1100;b.y=0;b.setPluginData('static-general-minimum-state','');const c=penpotUtils.findShape(s=>s.name==='General scroll content',b),wc=penpotUtils.findShape(s=>s.name==='General scroll content',wide);
if(['image','uploaded'].includes(key)){for(const n of ['span text 1','span community-settings-avatar'])c.children.find(s=>s.name===n).remove();for(const n of ['Synthetic Community avatar','Avatar white circle','Avatar orange stripe']){const original=wc.children.find(s=>s.name===n),x=original.x-wc.x,y=original.y-wc.y,t=original.clone();c.appendChild(t);t.x=c.x+x;t.y=c.y+y;}}
if(['uploaded','removed'].includes(key)){c.children.find(s=>s.name==='file content avatar No file chosen').remove();const t=wc.children.find(s=>s.name==='Avatar selected filename').clone();c.appendChild(t);t.x+=c.x+369-t.textBounds.x;t.y+=c.y+34.953125-t.textBounds.y;}
if(key!=='removed'){c.children.find(s=>s.name==='input avatar').resize(213.25,34);const button=wc.children.find(s=>s.name==='button Remove avatar').clone();c.appendChild(button);button.resize(94.75,50.78125);button.x=c.x+489.25;button.y=c.y+6.609375;const t=penpot.createText('Remove\navatar'),font=penpot.fonts.findAllByName('Inter').find(f=>f.name==='Inter');t.name='Remove avatar label';font.applyToText(t,font.variants.find(v=>v.fontWeight==='400'&&v.fontStyle==='normal'));t.fontSize='16';t.lineHeight='1.4';t.align='center';t.resize(78.75,50);t.growType='auto-height';t.fills=[{fillColor:'#FFFFFF',fillOpacity:1}];c.appendChild(t);t.x=c.x+497.25;t.y=c.y+10.609375;t.setPluginData('general-loaded-target',JSON.stringify({x:506.125,y:10.609375}));}
b.setPluginData('static-avatar-minimum-state',key);b.showInViewMode=false;for(const s of [b,...penpotUtils.findShapes(()=>true,b)])for(const a of [...s.interactions])s.removeInteraction(a);penpot.viewport.zoomIntoView([b]);return {key,id:b.id,remaining:keys.filter(k=>!penpot.currentPage.root.children.some(b=>b.name==='Desktop / Settings / avatar-small-'+k)).length};
