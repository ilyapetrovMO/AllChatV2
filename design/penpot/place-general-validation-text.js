const stage=penpot.currentPage.root.children.find(b=>b.name==='General validation text rendering stage');if(!stage)throw Error('Missing stage');const out=[];for(const t of [...stage.children]){const p=penpot.currentPage.getShapeById(t.getPluginData('validation-original-parent')),m=JSON.parse(t.getPluginData('general-validation-target')),n=t.clone();n.resize(t.name==='Native validation message'?p.width-48:250,24);p.appendChild(n);n.x+=p.x+m.x-n.textBounds.x;n.y+=p.y+m.y-n.textBounds.y;out.push({id:n.id,name:n.name,width:n.textBounds.width});}stage.remove();return out;
