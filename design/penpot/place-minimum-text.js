const stage=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / minimum-text-reference'),root=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / voice-minimum');if(stage.children.length!==43)throw Error('Incomplete reference');const bad=stage.children.filter(t=>!t.textBounds?.width||t.textBounds.width>Number(t.getPluginData('measured-width'))+1).map(t=>t.name);if(bad.length)throw Error('Unsettled text: '+bad.join(', '));for(const template of stage.children){const old=penpotUtils.findShape(s=>s.name===template.name,root),t=template.clone();old.parent.appendChild(t);t.name=old.name;t.x=old.x;t.y=old.y;old.remove();}stage.remove();return {replaced:43,id:root.id};
