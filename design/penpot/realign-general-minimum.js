const out=[];for(const b of penpot.currentPage.root.children.filter(b=>b.getPluginData('static-general-minimum-state'))){const c=penpotUtils.findShape(s=>s.name==='General scroll content',b);for(const t of c.children.filter(s=>s.type==='text'&&s.getPluginData('general-loaded-target'))){const m=JSON.parse(t.getPluginData('general-loaded-target'));const dx=c.x+m.x-t.textBounds.x,dy=c.y+m.y-t.textBounds.y;if(Math.abs(dx)>.1)t.x+=dx;if(Math.abs(dy)>.1)t.y+=dy;}out.push(b.id);}return out;
