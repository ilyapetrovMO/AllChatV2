const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-minimum-state'));for(const b of boards){b.x=100;b.y=100;for(const name of ['Noise suppression label','Input sensitivity output']){const t=penpotUtils.findShape(s=>s.name===name,b);t.growType='auto-width';t.resize(140,18);if(name==='Noise suppression label')t.getRange(0,t.characters.length).fontWeight='800';}}penpot.viewport.zoomIntoView(boards);return {refined:boards.length};
