const boards=penpot.currentPage.root.children.filter(b=>b.getPluginData('static-voice-settings-state'));let count=0;for(const b of boards){for(const t of penpotUtils.findShapes(s=>s.type==='text',b)){const size=t.fontSize;const r=t.getRange(0,t.characters.length);r.fontSize=String(Number(size)+0.01);r.fontSize=size;if(t.fills?.[0]?.fillColor)r.fills=[{fillColor:t.fills[0].fillColor,fillOpacity:t.fills[0].fillOpacity??1}];count++;}}return {refreshed:count};
