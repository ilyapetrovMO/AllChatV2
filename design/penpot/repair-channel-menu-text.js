const b=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / notifications-channel-menu');const reference=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / notifications-overrides');const menu=penpotUtils.findShape(s=>s.name==='Native select menu',b);for(const [i,label] of ['Community default','All Messages','Only @mentions','Nothing'].entries()){const old=penpotUtils.findShape(s=>s.name==='Option '+label,menu);const source=label==='Community default'?penpotUtils.findShape(s=>s.name==='Channel level general value',b):penpotUtils.findShape(s=>s.type==='text'&&s.characters===label&&s.name.startsWith('Channel level '),reference);const t=source.clone();t.name='Option '+label;menu.appendChild(t);t.x=menu.x+14;t.y=menu.y+5+i*27;t.fontSize='16';t.resize(192,24);t.fills=[{fillColor:i===0?'#303638':'#F0F1F5',fillOpacity:1}];t.getRange(0,t.characters.length).fills=[{fillColor:i===0?'#303638':'#F0F1F5',fillOpacity:1}];old.remove();}return {repaired:true};
