let stage=penpot.currentPage.root.children.find(b=>b.name==='Avatar text rendering stage');if(stage)return {stage:stage.id};stage=penpot.createBoard();stage.name='Avatar text rendering stage';stage.resize(500,400);stage.x=100;stage.y=100;stage.fills=[];let i=0;for(const b of penpot.currentPage.root.children.filter(b=>b.getPluginData('static-community-avatar-state'))){const c=penpotUtils.findShape(s=>s.name==='General scroll content',b);for(const t of [...c.children].filter(s=>['Remove avatar label','Avatar selected filename'].includes(s.name))){t.setPluginData('avatar-original-parent',c.id);stage.appendChild(t);t.growType='auto-height';t.resize(350,24);t.x=120;t.y=120+i++*40;}}penpot.viewport.zoomIntoView([stage]);return {count:i,stage:stage.id};
