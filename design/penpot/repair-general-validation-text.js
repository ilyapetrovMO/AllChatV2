let stage=penpot.currentPage.root.children.find(b=>b.name==='General validation text rendering stage');if(stage)return {stage:stage.id};stage=penpot.createBoard();stage.name='General validation text rendering stage';stage.resize(800,650);stage.x=100;stage.y=100;stage.fills=[];let i=0;for(const b of penpot.currentPage.root.children.filter(b=>b.getPluginData('static-general-validation-state'))){for(const t of penpotUtils.findShapes(s=>s.type==='text'&&s.getPluginData('general-validation-target'),b)){const parent=penpotUtils.findShape(s=>s.name===(t.name==='Native validation message'?'Native validation bubble':'General scroll content'),b);t.setPluginData('validation-original-parent',parent.id);stage.appendChild(t);t.growType='auto-height';t.resize(700,24);t.x=120;t.y=120+i++*45;}}penpot.viewport.zoomIntoView([stage]);return {count:i,stage:stage.id};
