const b=penpot.currentPage.root.children.find(b=>b.getPluginData('static-safety-dialog-state')==='safety-delete-confirm');let stage=penpot.currentPage.root.children.find(b=>b.name==='Desktop / Settings / safety-dialog-text-reference');if(!stage){stage=penpot.createBoard();stage.name='Desktop / Settings / safety-dialog-text-reference';stage.resize(400,100);stage.x=100;stage.y=100;stage.showInViewMode=false;const t=penpotUtils.findShape(s=>s.name==='Confirmation question',b);stage.appendChild(t);t.growType='auto-height';t.resize(350,24);t.x=120;t.y=120;}stage.bringToFront();penpot.viewport.zoomIntoView([stage]);return {id:stage.id,text:stage.children.map(t=>({copy:t.characters,font:t.fontFamily,bounds:t.textBounds}))};
