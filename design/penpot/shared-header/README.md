# Shared header controls

Penpot local-library component: `Desktop / Shared / Header controls`.

Component ID: `0c38e92e-1d02-8058-8008-9cceda4ac329`.

The main instance is on Desktop — Main, above the screen collection. It contains notifications, pinned messages, the members toggle, and search. Its 350 × 48 px instances are anchored to the header's right edge.

There are 44 linked instances: 31 on Desktop — Main, 7 on the logged-in prototypes on Desktop — Login, and 6 on Desktop — Voice Rooms. Full-screen Settings layouts do not contain this channel toolbar.

Existing prototype actions, member selection fills, and search values are retained as instance overrides. Update the main component to change shared appearance. The included SVG and PNG are visual reference exports; native component links are stored in Penpot.

Penpot version checkpoint: `Shared header controls — 44 linked instances — 2026-09-09`.
