if(penpot.currentPage.name!=='02 — Desktop')throw Error('Wrong page');
return penpot.currentPage.root.children.filter(b=>b.name.startsWith('Desktop widgets /')).map(b=>{let texts=0,markers=0,images=0;function walk(s){if(s.type==='text')texts++;images+=(s.fills||[]).filter(f=>f.fillImage).length;if(s.type==='rectangle'&&s.width===1&&s.height===1&&/^#f[de]/i.test(s.fills?.[0]?.fillColor||''))markers++;for(const c of s.children||[])walk(c);}walk(b);return {boardId:b.id,name:b.name,texts,images,markers,rootChildren:b.children.length,status:'Structure check only; visual and export verification pending'};});
